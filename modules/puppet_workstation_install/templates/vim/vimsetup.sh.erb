#!/bin/bash

set -e

workdir=<%= @builddir %>
if [ ! -d "${workdir}" ]; then
    mkdir $workdir
fi
cd $workdir > /dev/null
git -C $workdir/vim-setup pull || git clone https://github.com/mahnkong/vim-setup.git
cd vim-setup
sudo -u <%= @builduser %> ./setup.sh
